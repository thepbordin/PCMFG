---
phase: 03-overlay-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pcmfg/visualization/overlay_plotter.py
  - tests/test_overlay_plotter.py
  - tests/conftest.py
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "User can overlay 2+ normalized emotional trajectories on the same axes at equivalent progress points"
    - "User can generate a 9x2 grid (emotions x directions) with all narratives overlaid"
    - "User can generate per-emotion overlay plots for any of the 9 base emotions"
    - "User can generate per-direction overlay plots (A-to-B or B-to-A) for asymmetry analysis"
    - "When DTWClusterResult is provided, narrative lines are colored by cluster using CLUSTER_COLORS"
    - "When no DTWClusterResult is provided, narratives use tab10 sequential color cycle"
    - "Legends show cluster labels only (e.g., 'Cluster 0 (5 narratives)') not individual names"
  artifacts:
    - path: "pcmfg/visualization/overlay_plotter.py"
      provides: "NarrativeOverlayPlotter class with plot_overlay, plot_emotion, plot_direction"
      min_lines: 180
      contains: "class NarrativeOverlayPlotter"
    - path: "tests/test_overlay_plotter.py"
      provides: "Tests for VIS-01, VIS-02, VIS-03"
      min_lines: 120
      contains: "class TestVIS01"
    - path: "tests/conftest.py"
      provides: "sample_dtw_cluster_result fixture for VIS-04 tests"
      contains: "sample_dtw_cluster_result"
  key_links:
    - from: "pcmfg/visualization/overlay_plotter.py"
      to: "pcmfg/models/schemas.py"
      via: "import BASE_EMOTIONS, NormalizedTrajectory"
      pattern: "from pcmfg.models.schemas import BASE_EMOTIONS"
    - from: "pcmfg/visualization/overlay_plotter.py"
      to: "pcmfg/analysis/plotter.py"
      via: "import CLUSTER_COLORS for cluster coloring"
      pattern: "from pcmfg.analysis.plotter import CLUSTER_COLORS"
    - from: "pcmfg/visualization/overlay_plotter.py"
      to: "pcmfg/analysis/dtw_clusterer.py"
      via: "import DTWClusterResult for cluster data"
      pattern: "from pcmfg.analysis.dtw_clusterer import DTWClusterResult"
---

<objective>
Create the NarrativeOverlayPlotter class with core overlay methods (plot_overlay, plot_emotion, plot_direction) that overlay multiple normalized emotional trajectories on the same axes, with optional cluster coloring from DTWClusterResult.

Purpose: Enable cross-narrative emotional trajectory comparison by rendering all narratives on the same normalized timeline. This is the primary visualization capability — users see how different stories' emotional arcs compare side by side.

Output: Working overlay plotter class saving PNG files to disk, test scaffold with multi-narrative fixtures, and sample_dtw_cluster_result fixture for downstream VIS-04 tests.
</objective>

<execution_context>
@/Users/thepbordin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overlay-visualization/03-CONTEXT.md
@.planning/phases/03-overlay-visualization/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pcmfg/models/schemas.py:
```python
BASE_EMOTIONS = ["Joy", "Trust", "Fear", "Surprise", "Sadness", "Disgust", "Anger", "Anticipation", "Arousal"]

class NormalizedTrajectory(BaseModel):
    source: str
    main_pairing: list[str]
    direction: str  # 'A_to_B' or 'B_to_A'
    emotion: str    # from BASE_EMOTIONS
    x: list[float]  # uniform grid [0.0, 1.0]
    y: list[float]  # resampled values (integers 1-5)
    original_length: int
    n_points: int
    model_config = ConfigDict(frozen=True)
```

From pcmfg/analysis/dtw_clusterer.py:
```python
class DTWClusterResult(BaseModel):
    assignments: dict[str, int]  # source -> cluster label
    barycenters: list[NDArray[np.float64]]  # shape (n_points, 18) each
    distance_matrix: NDArray[np.float64]
    n_clusters: int
    metric: str
    sakoe_chiba_radius: int | None
    cluster_sizes: dict[str, int]
    silhouette_score: float | None
    sources: list[str]
    model_config = ConfigDict(frozen=True, arbitrary_types_allowed=True)
```

From pcmfg/analysis/plotter.py:
```python
CLUSTER_COLORS = [
    "#3498db", "#e74c3c", "#2ecc71", "#9b59b6", "#f39c12",
    "#1abc9c", "#e91e63", "#00bcd4", "#795548", "#607d8b",
]
```

From pcmfg/visualization/plotter.py (existing pattern to follow):
```python
class EmotionPlotter:
    def __init__(self, dpi: int = 300, figsize: tuple[float, float] = (16, 20)) -> None:
        self.dpi = dpi
        self.figsize = figsize
    # Methods save to file via fig.savefig(path, dpi=self.dpi, bbox_inches="tight") + plt.close(fig)
    # Axis formatting: ylim=[0.5,5.5], yticks=[1,2,3,4,5], xlim=[0,1], axhline(y=1)
    # Grid: plt.subplots(9, 2), iterate EMOTION_LIST
    # plt.tight_layout(rect=[0, 0, 1, 0.96]) for suptitle space
```

Barycenter column ordering (from build_dtw_dataset):
For each of 9 BASE_EMOTIONS, first A_to_B then B_to_A:
`[Joy_A2B, Joy_B2A, Trust_A2B, Trust_B2A, ...]`
Column index = `emotion_idx * 2 + dir_offset` (dir_offset=0 for A_to_B, 1 for B_to_A)

Existing fixture in tests/conftest.py:
- `sample_normalized_trajectories_multi`: 3 narratives ("rising_romance.txt", "enemies_to_lovers.txt", "slow_burn.txt"), 18 trajectories each (9 emotions x 2 directions), 100 points each
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NarrativeOverlayPlotter class with core overlay methods</name>
  <files>pcmfg/visualization/overlay_plotter.py</files>
  <behavior>
    - Class follows EmotionPlotter pattern: __init__(dpi=300, figsize=(16,20))
    - _group_trajectories(trajectories) returns dict[source, dict[(direction, emotion), NDArray]] — same pattern as build_dtw_dataset grouping
    - _unpack_barycenter(barycenter_18d) returns dict[(direction, emotion), NDArray] using column ordering emotion_idx*2+dir_offset
    - _plot_subplot_overlay(ax, y_arrays, colors, alpha, barycenter_y, x, emotion_label) renders multiple lines + optional barycenter on one subplot
    - plot_overlay(trajectories, output_path, cluster_result=None, title="Narrative Overlay"):
      Creates 9x2 grid, iterates BASE_EMOTIONS x ["A_to_B","B_to_A"], overlays all narratives per subplot
      When cluster_result provided: colors by CLUSTER_COLORS[cluster_label % len], alpha=0.3, legend shows "Cluster N (M narratives)"
      When None: colors by tab10, alpha=0.7, no cluster legend
      Barycenter shown when cluster_result provided (D-12): solid line, linewidth=3.5, diamond markers, alpha=1.0, zorder=10
      Saves to output_path with overlay_ prefix, returns None
    - plot_emotion(trajectories, emotion, output_path, cluster_result=None):
      Creates 1x2 subplot (A_to_B, B_to_A) for single emotion
      Same coloring/barycenter rules as plot_overlay
    - plot_direction(trajectories, direction, output_path, cluster_result=None):
      Creates 9x1 subplot for single direction (all 9 emotions)
      Same coloring/barycenter rules as plot_overlay
    - All methods: fig.savefig(path, dpi=self.dpi, bbox_inches="tight") + plt.close(fig)
    - Axis formatting: ylim=[0.5,5.5], yticks=[1,2,3,4,5], xlim=[0,1], axhline(y=1, gray, dashed, alpha=0.7), grid(True, alpha=0.3)
    - Log warning when only 1 narrative provided (allow it, don't raise)
  </behavior>
  <action>
    Create `pcmfg/visualization/overlay_plotter.py` with:
    1. Module docstring
    2. Imports: Path, Optional, logging, plt, np, DTWClusterResult, CLUSTER_COLORS, BASE_EMOTIONS, NormalizedTrajectory
    3. `logger = logging.getLogger(__name__)`
    4. `NarrativeOverlayPlotter` class with:
       - `__init__(self, dpi=300, figsize=(16,20))` — stores dpi, figsize (D-04)
       - `_group_trajectories(self, trajectories)` — groups flat list by (source, direction, emotion) using defaultdict(dict), returns dict mapping source -> dict[(direction, emotion)] -> np.array(y)
       - `_unpack_barycenter(self, barycenter)` — takes shape (n_points, 18) array, returns dict[(direction, emotion)] -> np.array of shape (n_points,) using `col = emotion_idx * 2 + dir_offset`
       - `_plot_subplot_overlay(self, ax, y_arrays, colors, alpha, barycenter_y, x, label)` — plots member lines (linewidth=1, alpha), optional barycenter (linewidth=3.5, marker="D", markersize=6, markevery=max(1,len(x)//10), alpha=1.0, zorder=10, label="Barycenter"), baseline (axhline y=1), axis formatting
       - `plot_overlay(self, trajectories, output_path, cluster_result=None, title="Narrative Overlay")` — 9x2 grid (D-01), saves as PNG (D-05, D-16), overlay_ prefix not added here (caller handles naming per D-15)
       - `plot_emotion(self, trajectories, emotion, output_path, cluster_result=None)` — 1x2 grid for one emotion
       - `plot_direction(self, trajectories, direction, output_path, cluster_result=None)` — 9x1 grid for one direction
    5. Cluster coloring logic (shared across methods):
       - If cluster_result: color = CLUSTER_COLORS[assignments[source] % len(CLUSTER_COLORS)], alpha=0.3
       - If None: color = plt.get_cmap("tab10")(i % 10), alpha=0.7
    6. Legend logic: When cluster_result, create legend entries as "Cluster {label} ({count} narratives)" using cluster_sizes. Place legend with loc="upper left", bbox_to_anchor=(1.02, 1) to avoid overlap.
    7. Single narrative warning: `if len(sources) < 2: logger.warning("Overlay with %d narrative — comparison is limited", len(sources))`
  </action>
  <verify>
    <automated>python -c "from pcmfg.visualization.overlay_plotter import NarrativeOverlayPlotter; print('OK')" 2>&1</automated>
  </verify>
  <done>NarrativeOverlayPlotter class imports cleanly with plot_overlay, plot_emotion, plot_direction methods</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffold with fixture and VIS-01/02/03 tests</name>
  <files>tests/conftest.py, tests/test_overlay_plotter.py</files>
  <behavior>
    - sample_dtw_cluster_result fixture: 2 clusters, cluster 0 has "rising_romance.txt" + "slow_burn.txt", cluster 1 has "enemies_to_lovers.txt". Barycenters are shape (100, 18) numpy arrays. Uses sources from sample_normalized_trajectories_multi fixture.
    - TestVIS01::test_overlay_creates_file: plot_overlay() creates PNG file at output_path, file exists
    - TestVIS01::test_overlay_multiple_narratives: plot_overlay() with 3 trajectories produces file
    - TestVIS01::test_overlay_with_cluster_coloring: plot_overlay() with cluster_result uses CLUSTER_COLORS
    - TestVIS01::test_overlay_without_cluster_uses_tab10: plot_overlay() without cluster_result uses default coloring
    - TestVIS02::test_plot_emotion_creates_file: plot_emotion("Joy", ...) creates PNG
    - TestVIS02::test_plot_all_emotions: Can call plot_emotion for all 9 BASE_EMOTIONS without error
    - TestVIS03::test_plot_direction_creates_file: plot_direction("A_to_B", ...) creates PNG
    - TestVIS03::test_plot_direction_btoa: plot_direction("B_to_A", ...) creates PNG
  </behavior>
  <action>
    1. Add to `tests/conftest.py`:
       - Import `DTWClusterResult` from `pcmfg.analysis.dtw_clusterer` (add to existing imports)
       - Create `sample_dtw_cluster_result` fixture that:
         - Depends on `sample_normalized_trajectories_multi` fixture
         - Creates 2-cluster assignment: cluster 0 = ["rising_romance.txt", "slow_burn.txt"], cluster 1 = ["enemies_to_lovers.txt"]
         - Creates barycenters: list of 2 numpy arrays, each shape (100, 18), dtype float64
         - Barycenter values: cluster 0 barycenter = mean of rising_romance + slow_burn trajectories (flat 1s with slight rise), cluster 1 barycenter = enemies_to_lovers trajectory values
         - Creates distance_matrix: 3x3 symmetric numpy array
         - Returns DTWClusterResult with all required fields (n_clusters=2, metric="dtw", sakoe_chiba_radius=2, cluster_sizes={"0": 2, "1": 1}, silhouette_score=0.5, sources=["enemies_to_lovers.txt", "rising_romance.txt", "slow_burn.txt"])

    2. Create `tests/test_overlay_plotter.py` with:
       - Standard test imports (pytest, Path, tmp_path)
       - Import NarrativeOverlayPlotter, BASE_EMOTIONS, CLUSTER_COLORS
       - TestVIS01 class (VIS-01: overlay trajectories):
         - `test_overlay_creates_file`: Use tmp_path, call plot_overlay with sample_normalized_trajectories_multi, assert PNG exists
         - `test_overlay_multiple_narratives`: Same as above, verify file size > 0
         - `test_overlay_with_cluster_coloring`: Call with sample_dtw_cluster_result, verify file created
         - `test_overlay_without_cluster_uses_tab10`: Call without cluster_result, verify file created
       - TestVIS02 class (VIS-02: per-emotion plots):
         - `test_plot_emotion_creates_file`: plot_emotion("Joy", ...) with tmp_path, assert PNG exists
         - `test_plot_all_emotions`: Loop over BASE_EMOTIONS, call plot_emotion for each, all create files
       - TestVIS03 class (VIS-03: per-direction plots):
         - `test_plot_direction_creates_file`: plot_direction("A_to_B", ...) with tmp_path, assert PNG exists
         - `test_plot_direction_btoa`: plot_direction("B_to_A", ...) with tmp_path, assert PNG exists
       - All tests use `tmp_path` fixture for output directory
       - Tests verify file existence and non-zero size, NOT pixel-level rendering
  </action>
  <verify>
    <automated>pytest tests/test_overlay_plotter.py -x -v 2>&1</automated>
  </verify>
  <done>All VIS-01, VIS-02, VIS-03 tests pass; sample_dtw_cluster_result fixture available for VIS-04</done>
</task>

</tasks>

<verification>
- `python -c "from pcmfg.visualization.overlay_plotter import NarrativeOverlayPlotter"` succeeds
- `pytest tests/test_overlay_plotter.py -x -v` — all VIS-01, VIS-02, VIS-03 tests pass
- `pytest tests/ -x` — full test suite passes (no regressions)
</verification>

<success_criteria>
- NarrativeOverlayPlotter class with plot_overlay(), plot_emotion(), plot_direction() implemented
- All three methods save PNG files to disk (not return Figure objects)
- 9x2 grid layout matches EmotionPlotter conventions
- Cluster coloring works with CLUSTER_COLORS when DTWClusterResult provided
- tab10 fallback when no cluster result
- Legends show cluster labels only, not individual narrative names
- sample_dtw_cluster_result fixture available in conftest.py
- All VIS-01, VIS-02, VIS-03 tests pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-visualization/03-01-SUMMARY.md`
</output>
